# 📦 仓库管理系统 - 保存成果指南

## 🎯 方案一：上传到 GitHub（推荐）

### 1. 在 GitHub 创建仓库

1. 访问 https://github.com
2. 点击右上角 `+` → `New repository`
3. 填写仓库名称（如：`warehouse-management`）
4. 选择 `Private`（私有）或 `Public`（公开）
5. 点击 `Create repository`

### 2. 在本地准备代码（Windows）

打开 PowerShell 或 CMD，进入项目目录：

```bash
cd E:\ck

# 初始化 Git（如果还没初始化）
git init

# 添加所有文件
git add .

# 提交代码
git commit -m "初始提交：仓库管理系统"

# 添加远程仓库（替换成你的 GitHub 仓库地址）
git remote add origin https://github.com/你的用户名/warehouse-management.git

# 推送到 GitHub
git branch -M main
git push -u origin main
```

### 3. 如果提示需要登录

GitHub 现在使用 Personal Access Token：

1. 访问：https://github.com/settings/tokens
2. 点击 `Generate new token` → `Generate new token (classic)`
3. 勾选 `repo` 权限
4. 生成后复制 token
5. 推送时用户名填你的 GitHub 用户名，密码填 token

---

## 💾 方案二：备份数据库（重要！）

### 在服务器上备份

```bash
# 进入后端目录
cd /opt/warehouse-management/backend

# 备份数据库
cp warehouse.db warehouse.db.backup.$(date +%Y%m%d)

# 或者压缩备份
tar -czf warehouse-backup-$(date +%Y%m%d).tar.gz warehouse.db
```

### 下载到本地

使用 WinSCP 或 FileZilla：
- 服务器路径：`/opt/warehouse-management/backend/warehouse.db`
- 下载到本地保存

### 定期自动备份

我已经为你创建了 `备份数据库.sh` 脚本，可以设置定时任务：

```bash
# 设置每天凌晨 2 点自动备份
crontab -e

# 添加这一行
0 2 * * * /opt/warehouse-management/备份数据库.sh
```

---

## 📝 方案三：保存服务器配置

### 1. 保存 Nginx 配置

```bash
# 在服务器上
sudo cp /etc/nginx/sites-enabled/warehouse ~/nginx-warehouse-config.txt
```

### 2. 保存环境变量

```bash
# 在服务器上
cp /opt/warehouse-management/backend/.env ~/backend-env.txt
```

**⚠️ 注意**：`.env` 文件包含敏感信息，不要上传到 GitHub！

### 3. 保存 PM2 配置

```bash
# 在服务器上
pm2 save
cp ~/.pm2/dump.pm2 ~/pm2-config.json
```

---

## 🔒 重要文件说明

### ✅ 应该上传到 GitHub 的文件

- 所有源代码（`src/`、`frontend/src/`）
- 配置文件（`package.json`、`tsconfig.json`、`vite.config.ts`）
- 文档（`README.md`、部署指南等）
- `.gitignore` 文件

### ❌ 不要上传到 GitHub 的文件

- `.env` 文件（包含密码和密钥）
- `node_modules/` 文件夹（太大，用 `npm install` 重新安装）
- `dist/` 文件夹（构建产物，可以重新构建）
- `*.db` 数据库文件（包含敏感数据）
- 日志文件

### 📋 .gitignore 已包含的内容

项目中的 `.gitignore` 已经帮你排除了：
- `node_modules/`
- `dist/`
- `.env`
- `*.db`
- 日志文件

---

## 🚀 快速上传到 GitHub（一键脚本）

我已经为你创建了 `上传到GitHub.bat` 脚本，双击运行即可！

---

## 📊 备份清单

定期检查以下内容：

- [ ] 代码已上传到 GitHub
- [ ] 数据库已备份（`warehouse.db`）
- [ ] 服务器配置已保存（Nginx、PM2）
- [ ] 环境变量已记录（`.env` 内容，但不要上传）
- [ ] 部署文档已更新

---

## 🔄 更新代码流程

### 本地修改后

```bash
cd E:\ck
git add .
git commit -m "描述你的修改"
git push
```

### 服务器上更新

```bash
cd /opt/warehouse-management
git pull
cd backend && npm install --production && npm run build
cd ../frontend && npm install && npm run build
pm2 restart warehouse-api
sudo systemctl restart nginx
```

或者使用我创建的 `更新代码.sh` 脚本：

```bash
chmod +x 更新代码.sh
./更新代码.sh
```

---

## 💡 最佳实践

1. **代码版本控制**
   - 每次重要修改都提交到 GitHub
   - 写清晰的 commit 信息

2. **数据库备份**
   - 每天自动备份
   - 重要操作前手动备份

3. **环境配置**
   - `.env` 文件不要上传
   - 在 README 中说明需要哪些环境变量

4. **文档更新**
   - 修改功能后更新 README
   - 记录部署过程中的问题

---

## 🎉 完成！

现在你的成果已经安全保存了！

