═══════════════════════════════════════════════════════
   仓库管理系统 - 云服务器部署快速指南
═══════════════════════════════════════════════════════

📋 部署步骤概览

1. 连接服务器（SSH）
2. 安装 Node.js
3. 上传代码到服务器
4. 运行部署脚本
5. 配置域名（可选）


🚀 方法一：使用自动化脚本（推荐，最简单）

1. 连接服务器
   ssh root@你的服务器IP

2. 上传项目代码到服务器
   方法A: 使用 Git
      cd /opt
      git clone 你的仓库地址 warehouse-management
   
   方法B: 使用 FTP/SFTP 工具上传

3. 运行部署脚本
   cd /opt/warehouse-management
   chmod +x deploy.sh
   ./deploy.sh

   脚本会自动完成：
   ✅ 安装 Node.js（如果没有）
   ✅ 安装项目依赖
   ✅ 构建前后端
   ✅ 配置 PM2 进程管理
   ✅ 配置 Nginx（如果已安装）
   ✅ 配置防火墙

4. 访问系统
   浏览器打开: http://你的服务器IP
   登录账号: admin / admin123


🔧 方法二：手动部署（详细步骤）

详见 "云服务器部署指南.md" 文件


📦 上传代码到服务器的方法

方法1: 使用 Git（推荐）
  在服务器上执行：
  cd /opt
  git clone 你的仓库地址 warehouse-management

方法2: 使用 FTP/SFTP 工具
  - Windows: WinSCP, FileZilla
  - Mac: Cyberduck
  - 上传整个项目文件夹到 /opt/warehouse-management

方法3: 使用 scp 命令（在本地电脑执行）
  scp -r ./warehouse-management root@服务器IP:/opt/


⚙️ 部署后配置

1. 修改管理员密码
   登录系统后立即修改默认密码

2. 配置域名（可选）
   - 在域名解析中添加 A 记录指向服务器IP
   - 配置 Nginx 的 server_name
   - 申请 SSL 证书（Let's Encrypt 免费）

3. 定期备份数据库
   cp /opt/warehouse-management/backend/warehouse.db /backup/warehouse-$(date +%Y%m%d).db


📝 常用管理命令

查看服务状态:
  pm2 list

查看日志:
  pm2 logs warehouse-api
  pm2 logs warehouse-frontend

重启服务:
  pm2 restart warehouse-api
  pm2 restart warehouse-frontend

停止服务:
  pm2 stop all

更新代码:
  cd /opt/warehouse-management
  git pull
  cd backend && npm install --production
  cd ../frontend && npm install && npm run build
  pm2 restart warehouse-api
  sudo systemctl restart nginx


❓ 遇到问题？

1. 查看详细部署文档: 云服务器部署指南.md
2. 查看 PM2 日志: pm2 logs
3. 查看 Nginx 日志: sudo tail -f /var/log/nginx/error.log
4. 检查服务状态: pm2 list


═══════════════════════════════════════════════════════
   祝你部署顺利！
═══════════════════════════════════════════════════════

